---

interface Props {
  className?: string
  id: string
}

const { className, id } = Astro.props

---

<article id={id} class={`px-4 py-8 w-full md:w-3/4 md:mx-auto ${className && className}`}>
  <slot />
</article>