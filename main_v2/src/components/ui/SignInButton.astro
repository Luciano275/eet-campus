---
interface Props {
  onlyMobile?: boolean;
}

const { onlyMobile } = Astro.props;

---

<a
  href={import.meta.env.CAMPUS_PAGE}
  class={`btn btn-primary text-white ${onlyMobile ? 'flex md:hidden' : 'hidden md:inline-flex'}`}>Acceder</a
>